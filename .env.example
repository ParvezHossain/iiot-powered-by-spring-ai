# Copy to .env, append a generated MCP_API_KEY as described in README.md.
# Compose file separator below is for Linux/macOS/WSL.
COMPOSE_FILE=docker-compose.yml:docker-compose.ai.yml
APP_PORT=8080
POSTGRES_PORT=5432
OLLAMA_PORT=11434
POSTGRES_PASSWORD=iiot_dev
AGENT_MODEL=qwen2.5:1.5b
RAG_ANSWER_MODEL=qwen2.5:1.5b
# Gmail is optional; leave disabled until account and recipient are configured.
ALERTS_GMAIL_ENABLED=false
