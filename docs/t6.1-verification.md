# T6.1 verification — 2026-09-08

Verification resumed from the interrupted working tree; existing README, AI
Compose override, `.env.example`, and ignore rules were retained.

## Environment and startup

- Existing `iiot-powered-by-ai` and `iiot-t61-docs` containers were stopped when
  inspected and were left untouched.
- The old test's `models` container exited 0; its logs show completed embedding
  and chat downloads, including digest verification and manifest creation.
- New project `iiot-t61-verify` used a newly created PostgreSQL volume and ports
  18080/15432/21434. Only the old test's downloaded Ollama volume was reused,
  through a temporary external-volume override. No model blobs were redownloaded.
- `docker compose up --build -d --wait --wait-timeout 900` (with the isolated
  project/env/volume selections) exited 0. The image build reused existing layers;
  the earlier clean build was not repeated.
- `.env.example`'s `COMPOSE_FILE` independently resolved all four services.
- Model initialization exited successfully at 15:57:05.903 UTC; the app started
  at 15:57:06.245 UTC. Required models: `nomic-embed-text:v1.5` (274 MB) and
  `qwen2.5:1.5b` (986 MB).
- Readiness returned 503 / OUT_OF_SERVICE while ingestion ran, then 200 / UP.
  Ingestion logged completion at 15:57:50.600 UTC. SQL confirmed 34 chunks from
  10 documents. PostgreSQL 17.10 and pgvector 0.8.6 were active.

## Runtime checks

- REST machine status returned SIM-001 and current samples for all seven metrics.
- REST anomaly query returned 200; initial warm-up result was empty. Subsequent
  injected spike/dropout alerts appeared in logs and persistent alert records.
  Email status was NOT_REQUESTED; Gmail credentials were empty and email disabled.
- Document search returned the E204 temperature-sensor reference as its top hit.
- REST RAG returned the correct E204 definition with validated citations and
  `insufficientEvidence=false` (23.8 seconds).
- MCP rejected missing/wrong bearer keys with 401; initialization, discovery,
  and all three tool calls succeeded. `ragQuery` returned a cited E204 answer.
- The README's MCP Inspector CLI discovery command exited 0 and listed exactly
  `getMachineStatus`, `getRecentAnomalies`, and `ragQuery`.
- The README's mixed chat question returned 200 after 152.5 seconds, but no tool
  evidence and `insufficientEvidence=true`. Its first follow-up reused the
  conversation ID and returned the same fallback. The last-week follow-up and
  separate knowledge chat also returned no tool evidence. This is not a
  successful mixed-answer or semantic-memory demonstration.
- A separate latest-vibration chat request successfully called `resolveMachine`
  and `getMachineStatus`, then failed final citation validation (200 with
  insufficient evidence, 112.7 seconds). Live telemetry tool calling is verified;
  successful retrieval through the live agent is not established.

## Documentation and automated checks

All README shell blocks passed Bash syntax validation and were checked against
repository paths, application configuration, and resolved Compose services.
Local Markdown links exist; `git diff --check` passed. Git clone, destructive
volume reset, and real Gmail sending were not executed. Gmail settings match the
mail sender implementation and the linked Google app-password requirements;
SMTP delivery remains unverified without an account and authorized recipient.

`./mvnw --batch-mode --no-transfer-progress verify` passed: 83 tests, no failures
or errors, one opt-in pgvector test skipped. Live pgvector ingestion was checked
separately above. `python3 scripts/demo.py` passed in 37.6 seconds with scripted
model/retrieval fixtures. The initial sandboxed demo attempt failed because
Mockito could not attach to its JVM; the unchanged command passed outside that
sandbox. No real email was sent.

## Resume fix

The first stop/resume attempt exposed a registry dependency: `ollama pull` of an
already installed model failed with HTTP 523 from the model registry, preventing
app startup. The final helper uses `ollama show` to reuse installed models and
pulls only missing models. README now documents cache reuse and explicit updates.
The unchanged dependency gate still requires successful initialization.

Resume with the final helper exited 0, logged both installed models, and retained
34 chunks / 10 documents. A shell harness exercised the exact helper with stubbed
Ollama responses: cached models cause no pulls; missing models are pulled; distinct
agent/RAG models are both initialized; a failed pull stops initialization with a
nonzero exit. Fresh-download completion is supported by the retained original
Docker logs, rather than a redundant download during this continuation.

The final Compose version was then rerun with another empty verification database
and the retained model cache: build/up/wait exited 0, readiness was UP, and SQL
again reported 34 chunks / 10 documents. Only the disposable verification project
and its database volume were removed afterward; the external model cache and
pre-existing projects were preserved.

## Acceptance

The stated startup criterion is satisfied: following README configuration and
Compose startup brings up the complete local stack without Gmail credentials.
The broader live-answer demonstration is limited: telemetry tool execution and
direct REST/MCP RAG are verified, but agent knowledge/mixed routing and validated
final chat answers did not pass. This verification does not certify those behaviors.
